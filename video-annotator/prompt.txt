Build a tool to annotate when different sorts of events happen in a video. I’d like an html interface where I can first either upload a video from my computer or provide a url where the video can be downloaded from. Show a standard play/pause/seek slider at the bottom of the video, a video display size adjustment slider above the video, and then a small interface shown below the video where the user can:
- Define event names they want to use
- Configure each event as being either a single frame label or a frame range label
- Configure keyboard shortcuts for adding the label (shortcut should default to the first letter of the annotation name) to the currently shown video frame of the video
- See a full list of which frames have which annotations
- Download the annotations in some text file format that’s both easy to read by eye and into python
- Upload annotations from the same file format to overwrite the current state of the label widget
Keep the page completely minimal other than these features


